//Print Array in reverse order
//INPUT - 3 1 2 3
//OUTPUT- 3 2 1
#include<stdio.h>
	int main()
	{
	  int a[10],i,n;
	  //Read size of the array and the array elements
	  scanf("%d",&n);
	  for(i=0;i<n;i++)
	  scanf("%d",&a[]);                 //Correct the code
	  
	  //Display the Array elements in reverse order
	  for(i=;i;i)                       //Correct the code
	  printf("%d ",a[i]);
	  return(0);
	}
